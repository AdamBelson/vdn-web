
{% if items %}
  <ul {{ attributes.addClass('fr-badges-group') }}>
    {% set items = items and items is not sequence ? [items] : items %}
    {% for item in items %}
      <li>{{ item }}</li>
    {% endfor %}
  </ul>
{% endif %}
